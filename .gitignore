/node_modules
/public/assets
# Every .env variant, not just the exact name: /.env alone did not cover
# .env.bak, which is how a backup carrying live keys got committed.
.env
.env.*
!.env.example
/data-pipeline/raw

# Playwright artifacts
test-results/
playwright-report/


# Vite build output (Vercel builds from source; committing this doubled every data refresh)
build/

.env.bak
